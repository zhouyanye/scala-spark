Seq（序列）：
Seq是一个通用的序列抽象，表示按顺序排列的元素集合。
Seq可以具体实现为不同的子类型，如List、Vector、Array等。
它提供了按索引访问、添加、更新和删除元素的方法。
List（列表）：
List是Seq的一个具体子类型，它表示一个按顺序排列的不可变元素列表。
List提供了高效的头部添加和尾部添加操作，但对于中间插入和删除操作，效率较低。
由于List是不可变的，每次操作都会生成新的列表，而不会修改原始列表。
Map（映射）：
Map是一种键值对（key-value）的映射数据结构，其中的键是唯一的。
Map用于将键与值关联起来，通过键来访问对应的值。
可以有不同的实现，如HashMap、TreeMap等，每种实现都具有不同的性能特点。
Set（集合）：
Set是一个无序的、不允许重复元素的集合数据结构。
Set用于存储一组唯一的元素，常用于检查元素的存在性。
同样可以有不同的实现，如HashSet、TreeSet等。